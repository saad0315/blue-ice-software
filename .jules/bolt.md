## 2024-05-15 - [Dashboard In-Memory Optimization]\n**Learning:** The dashboard `GET /` endpoint in `src/features/dashboard/server/route.ts` derived `orderCount`, `activeOrderCount`, and `totalRevenue` from separate database queries.\n**Action:** Replaced separate queries by deriving metrics in-memory from the `orderStatusDistribution` (`db.order.groupBy`) query and the `revenueData` aggregation to minimize database requests, but wait, I can derive `activeOrderCount` and `orderCount` from `orderStatusDistribution`. `totalRevenue` is currently its own aggregate query but maybe it can be merged too. Wait, `totalRevenue` is sum of totalAmount for COMPLETED orders. If I add `_sum: { totalAmount: true }` to the `groupBy`, I can derive all 3 from the single `groupBy` query!
